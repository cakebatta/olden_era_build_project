# BE-012 Self-Contained Patcher

Place `apply_be_012.py` in the repository root:

`C:\Users\BB BOBBY\Documents\GitHub\olden_era_build_project`

Then run:

```powershell
python apply_be_012.py
```

This patcher embeds all BE-012 source, test, and report content directly.
It does not require `_be012_payload` or any companion folders.
