# Coding Standards

- Prefer explicit, typed dataclasses.
- One responsibility per module.
- Avoid hidden global state.
- Reusable library code must not depend on repository layout.
- Tests belong in `scripts/`.
- Validate inputs early with informative exceptions.
- Preserve backward compatibility where practical.
- Keep functions small and composable.
- Prefer readability over cleverness.
